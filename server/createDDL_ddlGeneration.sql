CREATE TABLE CHARACTER (ID INTEGER NOT NULL, LEVEL INTEGER, NAME VARCHAR, ROLE VARCHAR, PRIMARY KEY (ID))
CREATE TABLE WEAPON (ID INTEGER NOT NULL, DAMAGETYPE VARCHAR, NAME VARCHAR, TYPE VARCHAR, PRIMARY KEY (ID))
CREATE TABLE CHARACTER_WEAPON (Character_ID INTEGER NOT NULL, weapons_ID INTEGER NOT NULL, PRIMARY KEY (Character_ID, weapons_ID))
ALTER TABLE CHARACTER_WEAPON ADD CONSTRAINT FK_CHARACTER_WEAPON_Character_ID FOREIGN KEY (Character_ID) REFERENCES CHARACTER (ID)
ALTER TABLE CHARACTER_WEAPON ADD CONSTRAINT FK_CHARACTER_WEAPON_weapons_ID FOREIGN KEY (weapons_ID) REFERENCES WEAPON (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
